#include <bits/stdc++.h>
using namespace std;

int n, k, a[100002];
int main()
{
    cin >> n >> k;
    for (int i = 0; i < n; i++)
        cin >> a[i];
    nth_element(a, a + k - 1, a + n);
    cout << a[k - 1];
}